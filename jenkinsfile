pipeline {
    agent any

    environment {
        sh_ENV = 'test'
        ALLURE_RESULTS = './allure-results'
    }

    stages {
        stage('Install Dependencies') {
            steps {
                dir('./tests') {
                    npm ci
                }
            }
        }
    }

    post {
        always {
            echo 'Completed...'
        }
    }
}